spring:
  application:
    name: user-center
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    # 线上数据库的地址 ?useSSL=false
    url: jdbc:mysql://192.168.41.100:3306/user_center?useSSL=false
    username: root
    password: "0420"
  # session失效时间 一天86400
  session:
    timeout: 86400